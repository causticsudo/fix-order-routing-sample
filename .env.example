# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_postgres_password
POSTGRES_DB=fixorderrouting

# Keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme_keycloak_password

# Application
ASPNETCORE_ENVIRONMENT=Development

# Redis
REDIS_PASSWORD=

# Jaeger
JAEGER_ENABLED=true

# Note: NEVER commit secrets to version control
# Copy this file to .env and update with your local secrets
# .env is ignored by .gitignore
